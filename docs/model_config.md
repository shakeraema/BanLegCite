# model_config.md
# stage: Phase 5 — Reproducibility Prep
# produced_by: Researcher B
# date: 2026-07-15
# reviewed_by: pending (both researchers)
#
# DO NOT MODIFY after Phase 4 results are logged. Any change requires a
# joint decision-log entry per CLAUDE.md Section 5.

# Model & Prompt Configuration — BanLegit-Cite

> **STATUS: FROZEN**
> Frozen after Phase 4 baseline runs are complete.
> Any change requires a dated joint decision-log entry.

---

## Model Registry

| Model ID | Provider | Version | Role |
|----------|----------|---------|------|
| `gemini-1.5-flash` | Google DeepMind | 1.5-flash (latest at run date) | Primary baseline |
| `gemini-1.5-pro` | Google DeepMind | 1.5-pro (latest at run date) | Secondary baseline (Phase 6) |

---

## Evaluation Settings

| Setting | Description | Retrieval | Context Window |
|---------|-------------|-----------|----------------|
| `standard` | Zero-shot direct verification prompt | None | Citation + Paragraph context |
| `agentic` | Retrieval-augmented verification prompt | LocalRetriever (BM25-style index over `data/raw/`) | Citation + Paragraph + Retrieved Reference |

---

## Prompt Templates

### Standard Prompt
```
You are a legal citation verification assistant. Your task is to verify if the
following citation is REAL or FABRICATED.

A citation is REAL if it exists in the designated reporter volume and page,
and matches the legal holding.
A citation is FABRICATED if it does not exist, references the wrong
volume/page, or makes false claims.

Context Paragraph: {context}
Citation to Verify: {citation}
Purported Source: {source}

Output your analysis in this exact format:
Verdict: [REAL or FABRICATED]
Confidence: [1 to 5]
Reasoning: [Brief explanation of your verdict]
```

### Agentic Prompt
```
You are a legal citation verification assistant with access to verified legal
references. Use the following retrieved background information to evaluate if
the citation is REAL or FABRICATED.

Background Reference:
{retrieved_info}

Context Paragraph to Verify:
{context}
Citation to Verify: {citation}
Purported Source: {source}

Output your analysis in this exact format:
Verdict: [REAL or FABRICATED]
Confidence: [1 to 5]
Reasoning: [Brief explanation referencing the background document]
```

---

## Inference Parameters

| Parameter | Value | Notes |
|-----------|-------|-------|
| Temperature | default (1.0) | Not overridden — standard Gemini default |
| Max output tokens | default | Structured output is short; no cap needed |
| Batch size | ≤50 examples | Per CLAUDE.md batch-and-gate rules |
| Seed | N/A | Gemini API does not expose a seed parameter |

---

## Reproducibility Notes

- All experiment output files follow the naming convention:
  `<stage>_<model>_<setting>_<YYYYMMDD>_<run-time>.json`
- Python environment is pinned in `requirements.txt` (generated by `uv pip freeze`).
- Python version: **CPython 3.14.6** (via uv-managed virtual environment at `.venv/`)
- All prompts are version-controlled in `scripts/evaluation/prompts.py`.
- Retriever index is rebuilt deterministically from `data/raw/` on each run.
